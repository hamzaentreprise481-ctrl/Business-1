{
  "name": "rental-app-backend",
  "version": "1.0.0",
  "description": "Backend pour application de location de vêtements de marque (invendus magasins) - Firestore + Stripe + Vercel API Routes",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "vercel dev"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.7",
    "@vercel/node": "^3.1.6"
  },
  "engines": {
    "node": ">=18"
  }
}
